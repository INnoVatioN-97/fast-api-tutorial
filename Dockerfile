# 1. 베이스 이미지: 공식 Python 3.12-slim 이미지를 사용합니다.
FROM python:3.12-slim

# 2. 작업 디렉토리 설정: 컨테이너 내에서 작업할 기본 경로를 /app으로 설정합니다.
WORKDIR /app

# 3. 의존성 설치 (캐시 최적화)
# 먼저 의존성 파일만 복사하여 설치합니다.
# 이렇게 하면 소스 코드가 변경되어도 의존성이 동일할 경우, 이 레이어를 재사용하여 빌드 속도가 빨라집니다.
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. 소스코드 복사
# 나머지 소스 코드를 작업 디렉토리로 복사합니다.
COPY . .

# 5. 포트 노출
# 애플리케이션이 8000번 포트를 사용함을 명시합니다.
EXPOSE 8000

# 6. 애플리케이션 실행
# 컨테이너가 시작될 때 uvicorn 서버를 실행합니다.
# --host 0.0.0.0 옵션은 컨테이너 외부에서 애플리케이션에 접근할 수 있도록 해줍니다.
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
